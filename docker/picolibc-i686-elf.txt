# Meson cross file: build picolibc with the NanOS i686-elf cross-gcc.
# Used by docker/Dockerfile. The toolchain is already on PATH (/opt/cross/bin).
[binaries]
c = 'i686-elf-gcc'
ar = 'i686-elf-ar'
as = 'i686-elf-as'
strip = 'i686-elf-strip'

[host_machine]
system = 'none'
cpu_family = 'x86'
cpu = 'i686'
endian = 'little'

[properties]
# The cross-gcc cannot link a hosted test program (freestanding), so skip Meson's
# compiler sanity link check.
skip_sanity_check = true
