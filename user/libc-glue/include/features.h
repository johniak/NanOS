/* features.h — minimal stub for ports (busybox) that include <features.h> via newlib/_NEWLIB_VERSION.
 * NanOS is not glibc; we intentionally leave __GLIBC__ undefined so portable code takes the
 * generic/musl path. Empty by design. */
#ifndef _NANOS_FEATURES_H
#define _NANOS_FEATURES_H
#endif
