# Electron/MarkText Plan Status

Single source of truth for program status (README links here — do not duplicate checkboxes
elsewhere). Update after each gate, not only milestones. Keep entries factual: date, commit, gate,
result. Row format is defined in EXECUTION_GUIDE.md "Work Package Protocol".

## Milestones

- [x] M0 Source Freeze
- [x] M1 Platform Gap Matrix
- [ ] M2 Node — **`node.nxe --version` → `v24.15.0` RUNS on NanOS x86_64** (2026-07-14); reserve/commit VA model + worker-thread TLS added → node clears ALL of V8's VA reservations AND V8's heap CHECK; JS eval now blocked on a HANG later in V8/node startup (no crash)
- [ ] M3 Chromium Content
- [ ] M4 Electron Runtime
- [ ] M5 Generic Packager
- [ ] M6 MarkText Build
- [ ] M7 MarkText Run

## Log

| Date | Commit | Gate | Result |
|---|---|---|---|
| 2026-07-07 | _plan only_ | Plan package created | Pending execution |
| 2026-07-13 | _wip_ | M0 Source Freeze (LOCK-OK/DOC-OK/MATRIX-STUB-OK) | PASS — pinned MarkText 43bd8b7 (0.20.0-dev), Electron 42.1.0, Chromium 148.0.7778.97, Node v24.15.0; electron-stack.lock + electron-platform.md written. Fork locations adapted to NanOS-labs. |
| 2026-07-13 | _wip_ | M1 Task 1.1 Gap Matrix (re-verified vs tree) | PASS — matrix filled. missing-required: eventfd, epoll, pipe2, dup3, mprotect(real RW↔RX, currently no-op stub posixstubs.c:423), /proc/self/exe, /proc/self/fd. Tasks 1.2–1.5 (kernel impl) NOT started; M1 milestone still open. |
| 2026-07-13 | _wip_ | M1 Task 1.2 Event-loop primitives (smoke-eventfd) | PASS — eventfd2 + level-triggered epoll (kernel/Eventfd.h, kernel/Epoll.h, dispatch, libc-glue, sys/{eventfd,epoll}.h). test64 898 cases green; smoke-eventfd (eventfdtest+epolltest, -smp 4) PASS. Fixed a latent x86_64 ABI bug: read/write glue returned int → -1 not sign-extended to ssize_t. Tasks 1.3–1.5 remain. |
| 2026-07-13 | _wip_ | M1 Task 1.3 Real mprotect RW↔RX / V8 W^X (smoke-mprotect) | PASS — was a no-op stub. arch::mmuProtectUser toggles PTE_RW + TLB shootdown; AddressSpace::protect/leafEntry; SYS_mprotect; real userland wrapper. NX not enabled → PROT_EXEC no-op, write-protect via PTE_RW; absent pages skipped (no ENOMEM) to not regress pthread guard pages. test64 899 green (+protect host test); smoke-mprotect (mmapexectest) PASS. No-regression proven: smoke-x86_64 + smoke-smp-stress (8-thread) + smoke-eventfd all PASS isolated. NOTE: a full `make verify64` needs the stray run64-gl QEMU stopped (it holds hostfwd :2222, collides with net smokes). Tasks 1.4–1.5 remain. |
| 2026-07-13 | _wip_ | M1 Task 1.4 /proc/self/{exe,fd} (smoke-procself) | PASS — Process.exe (set at execve, fork-inherited) + ProcInfo.exe; ProcTable::{setExe,openFds,selfPid}; Syscalls::listOpenFds; SynthFs classifyProc `self`-resolution + exe symlink (readlink) + fd directory (readdir). test64 900 green (+setExe/selfPid host test); smoke-procself PASS. FOUND a separate gap (now in matrix): `/tmp` is searchable but NOT user-writable (open O_CREAT as jan → -1) — Node/Electron will need `/tmp` 1777; follow-up. Task 1.5 remains. |
| 2026-07-13 | _wip_ | M1 Task 1.5 groundwork: user-writable /tmp + memfd (smoke-memfd) | PARTIAL PASS — `/tmp` now mounts RamFs(01777) so non-root creates temp files (fixes the 1.4 gap); memfd_create keeps its /tmp entry linked (NanOS tracks fds by path → unlinked-open loses ftruncate/mmap), so ftruncate/write/read round-trip works. memfd_create prototype added to sys/mman.h. test64 901 green (+RamFs rootMode host test); smoke-memfd PASS. **REMAINING (Task 1.5 core):** cross-process MAP_SHARED writeback — a file mmap currently allocates fresh private pages (SyscallDispatch ~1023), so shmdualtest hangs (child never sees the parent's write). Needs page-frame-backed RamFs + shared mapping. shmdualtest.c is in-tree as the target. |
| 2026-07-13 | _wip_ | M1 Tasks 1.6/1.7/1.8 policy + data-file + sandbox docs | DONE (doc-only) — electron-platform.md now has: File Watching Policy (inotify off by decision → launcher sets CHOKIDAR_USEPOLLING=1, idle-CPU contract for smoke-marktext); Runtime Data Files (ICU/locales/snapshots under /disks/main/nanos/lib/electron; fonts /nanos/share/fonts + certs /nanos/ssl/cert.pem already in every image; deterministic Skia font dir); Chromium sandbox disabled (--no-sandbox launcher default + GN, security limitation, real-sandbox follow-up). Source data files verified present + staged by _image64. M1 milestone stays OPEN pending only the Task 1.5 MAP_SHARED core. |
| 2026-07-14 | _wip_ | **M2 Task 2.3 cont.⁴: worker-thread TLS — node clears V8's heap CHECK (crash → hang)** | Root-caused the previous blocker (`Check failed: AllowHeapAllocationInRelease::IsAllowed()`): `pthread_create` gave worker threads a **bare TCB with NO `__thread` TLS block** (it assumed "NanOS userland has no TLS image" — true until V8). V8's `current_per_thread_assert_data` is a `.tdata` thread_local initialised to a NON-zero value (0xFFFFFFDF = all asserts allowed); on a worker with no TLS block it read 0 → `IsAllowed()` false → CHECK abort during heap bring-up. Fix: `pthread_create` now builds a real per-thread TLS block via `__copy_tls` (the same musl machinery nx_tls.c uses for the main thread — copies the `.tdata` template + zeroes `.tbss`, sets `%fs`) when the program has TLS; **the no-TLS bare-TCB path is kept byte-for-byte** (reordering it wedged smptorture's 8-way mutex phase — regression caught + reverted). Gate: new **smoke-tls** (tlstest — 8 workers read `__thread` initializers, keep private TLS under contention) PASS; regression: smoke-smp-stress + smoke-resv green. **RESULT: node no longer crashes at the heap CHECK — it now HANGS silently later in V8/node startup (no output, no fault, machine alive).** NEXT (new, distinct): diagnose the startup hang (likely a thread-pool / condvar / futex wait or an I/O block in V8/libuv init on NanOS) — a synchronization/liveness issue, not TLS (tlstest proves per-thread TLS is correct) and not memory (reserve/commit + heap all fine). |
| 2026-07-14 | _wip_ | **M2 Task 2.3 cont.³: reserve/commit VA model — node clears ALL V8 VA reservations** | Fixes the documented SegmentedTable OOM AND the next one (CodeRange). NanOS had no way to reserve VA without eagerly backing every page with a frame, so V8's large PROT_NONE subspaces OOM'd. Added a **reserve-without-backing window**: `mmap(PROT_NONE + MAP_NORESERVE)` hands out VA in a HIGH window (above the identity-mapped RAM + the sub-4 GiB MMIO hole; base set at boot from topOfRam, ≥4 GiB, so it aliases neither the kernel byte heap at phys [0x20000000,0x40000000) nor MMIO) with **no frames**; `mprotect(RW)` or a `MAP_FIXED` anon over-map commits pages on demand (`arch::mmuCommitResv` alloc+zero+map; `mmuDecommitResv` frees). Window is 64-bit VA (arch mmu globals `g_resvBase/g_resvMax`, `Process::resvNext` bump-only; fork/teardown cover it via freeUserWindow/copyUserWindow). Also switched `SYS_mmap` to the **real Linux 6-arg ABI** (`addr,length,prot,flags,fd,offset`) so `addr` reaches the kernel — the old ABI dropped it, breaking `MAP_FIXED` (V8 DecommitPages) and silently mis-decoding every raw `syscall(SYS_mmap)`; fixed the mprotect/munmap glue truncating a ≥4 GiB `addr` to 32 bits. Gate: new **smoke-resv** (resvtest — reserve 128 MiB with **0 KiB RAM drop**, commit/decommit/re-commit, uncommitted pages fault, 40× teardown-with-committed-pages stress) PASS; no regression (smoke-mprotect/memfd/shmshare/x86_64/smp-stress all green). **RESULT: node advances past SegmentedTable AND CodeRange (previously died at the first reservation) into V8 isolate/heap setup.** NEXT (new, deeper): `node --jitless -e "console.log(1+2)"` now aborts at `Check failed: AllowHeapAllocationInRelease::IsAllowed()` during heap bring-up (a V8-internal allocation-in-disallowed-scope), followed by kernel HEAP-tag corruption during the crashed process's teardown — NOT the reserve/commit code (its teardown path is stress-clean), so either V8 heap/snapshot setup on NanOS or a latent kernel-heap bug newly reached by node's load. |
| 2026-07-14 | _wip_ | **M2 Task 2.3 DONE — `node.nxe --version` → `v24.15.0` RUNS on NanOS** 🎉 | The libuv `uv_mutex_unlock` EPERM (previous row) was a ONE-CHARACTER bug: `user/libc-glue/pthread/nx_tls.c` set the main thread's tid with `syscall(224)` — but **224 is i386's gettid; x86_64's is 186** (SyscallNr.h had it right). 224 returned -ENOSYS, so `self->tid = -38`; a non-NORMAL mutex (libuv's ERRORCHECK) then compared `own=(-38 & 0x3fffffff)=0x3fffffda` against `self->tid=0xffffffda` and returned EPERM even same-thread → `uv_mutex_unlock` abort(). NORMAL mutexes skip the owner check, which is exactly why the pthread port passed with the wrong number. Fixed to 186. **RESULT: node.nxe boots, initializes V8 + libuv, and prints `v24.15.0` — node runs on NanOS.** NEXT (distinct, architectural): actual JS eval (`node -p 67`) aborts with `Fatal process out of memory: SegmentedTable::InitializeTable (subspace allocation)`. NOTE: V8's sandbox + pointer-compression are ALREADY off in this build (tools/v8_gypfiles/features.gypi: `v8_enable_sandbox%=0`, `v8_enable_pointer_compression%=0`), so disabling them is not the fix — V8 13.6 still uses SegmentedTable for always-on pointer tables. The table reserves a large PROT_NONE VA **subspace** and commits sub-regions lazily; NanOS **eagerly backs every mapping with frames (no demand paging)**, so a big reservation OOMs. Path forward = teach NanOS mmap to RESERVE VA without allocating frames (PROT_NONE / MAP_NORESERVE), widen the user VA window, and fix the `SYS_mmap2` 32-bit length truncation (SyscallDispatch.cpp:971) — i.e. a real reservation/commit memory model. No regression: test64 912/912. |
| 2026-07-14 | _wip_ | **M2 Task 2.3 cont.²: node EXECUTES V8 + enters libuv init** (ctors run, TLS live, execve clean; aborts on a pthread mutex-owner error) | Building on the ctor/symbol work below: (1) the ctor-bracket linker script only works as node's DEFAULT script, not via -Wl,-T — ld resets the collected .ctors LMA to the base (overlaps .nxheader) under -T but not as the default. Root cause: the nanos gcc spec is `%{!T:-T nx.ld}`, so node's normal link already uses a NanOS `nx.ld`; passing -T *suppressed* it. Fixed by staging `ports/node/nx-node.ld` (= stock nx.ld + a `.nx_ctors` collection **+ the .tdata/.tbss TLS template** `__nx_tls_*`, mirroring user-nx.ld) OVER the sysroot nx.ld from the `make node` target — no -T. (2) This also fixed a KERNEL derail (`cs=0 rip=2`) that a new per-CPU **last-syscall ring** (dumped by the kernel fault handler) pinned to `SYS_execve` of node: the earlier -T script based on the generic default put node's base at 0x400000, loading a segment BELOW the user window; the nx.ld base 0x800000 fixes it. (3) With TLS now set up, node's main thread stopped taking the bare-TCB path; that had left `self->robust_list.head=0` → #PF (cr2=-8) in `__pthread_mutex_trylock_owner`. Also self-pointed `robust_list.head` in `pthread_create` for worker threads (musl does this; our port omitted it). **RESULT: node runs V8 platform init + its 148 static constructors, sets up real TLS, and enters libuv init — no segfault.** NEXT: libuv's `uv_mutex_unlock` calls `abort()` because `pthread_mutex_unlock` returns EPERM (a non-normal mutex whose stored owner tid ≠ `self->tid`) — a pthread/thread-identity issue to instrument next (per-lock tid, or the mutex type libuv uses). No regression: test64 912/912 (3.2M assertions), userland boots. |
| 2026-07-14 | _wip_ | **M2 Task 2.3 cont.: node runs its C++ static constructors + resolves ALL libc symbols; advances from earliest-init to V8 runtime** | The rip=0 crash was NOT the TLS relocs — it was three real bugs, now fixed: (1) **C++ symbol mangling**: the sysroot `<syscall.h>/<dlfcn.h>/<dirent.h>/<sys/ioctl.h>/<link.h>` declared C libc functions WITHOUT `extern "C"`, so node's C++ calls mangled (`_Z7syscalllz` = `syscall`) and linked to address 0 → `call 0` at rip=0 (found via a new guarded caller-dump in the ring-3 fault handler: `[rsp]`→`0x93d121`→`node::credentials::SafeGetenv`→`syscall(SYS_capget)`). Fixed all five headers `extern "C"`; the shipped libstdc++.a (built against the old headers) still references mangled `readdir/closedir/dirfd/fdopendir/ioctl` from `std::filesystem::_Dir`/`std::__basic_file`, resolved with signature-identical asm-labeled forwarders in posixstubs.c (removable once libstdc++ is rebuilt). (2) **missing libc functions** node calls GLOBAL-strong (would `call 0`): added `dup3/pipe2/readv/scandir/madvise/inotify_*/getgr{gid,nam}_r/pthread_atfork/__cxa_pure_virtual` (weak V8 hooks like `pkey_*`/`_ITM_*` are guarded by the caller and safely stay 0). Verified: ZERO GLOBAL-undefined symbols remain in the node ELF. (3) **static constructors never ran** (the actual rip=0-successor bug): node's ctors live in `.ctors`/`.ctors.NNNNN` but the crt0 bracket symbols `__nx_{init_array,ctors}_{start,end}` were WEAK-UNDEF=0 (gyp's default link doesn't use `user-nx.ld`), so crt0's ctor loops ran nothing → every C++ static-init global (`per_process::system_environment`) stayed null → null-deref in bootstrap. Fixed with `ports/node/nx-node.ld` (default script + contiguous `.ctors` collection + the `__nx_*` brackets) injected via `-Wl,-T` from the `nx-node-cxx` link wrapper; ELF now shows `__nx_ctors_start..end` bracketing a contiguous 148-entry `.ctors`. **RESULT: node now runs its static initializers and advances past V8 platform init + Environment/credentials setup — no more ring-3 crash.** NEXT (new, deeper): a KERNEL derail (`vec=0e cs=0 rip=2 rsp=0 cr2=2`) once node enters V8 runtime — a NanOS kernel bug exposed by V8's runtime behavior (thread creation / signal install / the multi-GiB memory cage), NOT a node-linking issue. No regression: test64 912/912 (3.2M assertions), normal userland boots + logs in. |
| 2026-07-14 | _wip_ | **M2 Task 2.3: node.nxe builds + links + LOADS + EXECUTES on NanOS** (crashes in early init) | MAJOR MILESTONE. The full Node/V8 cross build completes: `node` (61 MiB x86_64-nanos ELF) links, mknx→node.nxe (clean), stages into image64 (/nanos/bin/node.nxe + /apps/node-smoke), and under QEMU **loads + starts executing** — reaches early crt0/static-init then faults at rip=0x0 (null fn-ptr call, rsp near stack top = very early). Root of the load: the **threads wall was cleared by rebuilding the SDK GCC + libstdc++ with --enable-threads=posix** (compiler was thread-model=single from --without-headers), plus ~40 source-level porting fixes across openssl/abseil/highway/v8/libuv/node (compiler-flag wrappers; generic syscall(); a large libc-glue POSIX surface — sysinfo/sendfile/mremap/getifaddrs/recvmmsg/getauxval/prctl/mlock, pthread affinity/getattr_np/getname_np/kill/attr_setstack, full struct rusage; ~15 new headers; ~12 build defines; 6 source patches). To LOAD node the kernel needed: VA_USER_END 64→256 MiB, user stack 512 KiB→8 MiB, exec staging 32→128 MiB, libstdc++ rebuilt with the dllimport shim, in6addr_any local-linked. No regression: test64 912/912, normal userland boots+logs in. **NEXT: the rip=0 crash — node's ELF needs R_X86_64_TPOFF32/GOTTPOFF (TLS) relocations that tools/mknx.c does not apply; teach mknx the TLS relocs (details in docs/en/node-port.md).** |
| 2026-07-14 | _wip_ | M2 Tasks 2.1–2.3: Node/V8 fetch + cross-build bring-up | IN PROGRESS (blocked at a toolchain wall). Fetched nodejs/node v24.15.0 (electron/node has no such tag → upstream per Decisions fallback; v8 13.6.233.17, libuv 1.51.0). Full scaffolding written: fetch-node.sh, build-node.sh, node-smoke.js, smoke-node.sh, ports/node/{README,nx-node-cc/cxx wrappers,addon-smoke,patches}, docs/en/node-port.md. Cross-**configure OK**; cross-**build reaches: libuv host tools OK, OpenSSL target OK, into V8/abseil**. ~13 porting issues resolved: -pthread/-rdynamic compiler-wrapper strips; libc-glue — **generic syscall() multiplexer** (kernel uses Linux x86_64 numbers), `<syscall.h>`→SyscallNr.h, mlock/prctl, `<linux/futex.h>`, `<sys/ucontext.h>`+`<ucontext.h>`, `<sys/prctl.h>`; **nx-dllimport.h made C++-safe** (NX_IMP extern "C" — prior ports were all C); build defines -D__linux__ / -D_GNU_SOURCE / -D_POSIX_SEM_VALUE_MAX; 2 abseil patches (raw-mmap→libc mmap, std::wcslen→::wcslen); resource tuning (-g0/-j4/14g). **WALL: the SDK libstdc++ is --enable-threads=single (`_GLIBCXX_HAS_GTHREADS` undefined → no std::mutex/thread/condition_variable) AND the sysroot libc.a has no pthread symbols.** V8/Node need C++ threading. Next phase = rebuild libstdc++ --enable-threads=posix + stage the NanOS pthread port into the sysroot (build-gcc tree present). Details: docs/en/node-port.md. No NanOS regression: libc-glue changes are additive/superset (generic syscall, new headers, C-identical NX_IMP). |
| 2026-07-13 | _wip_ | **M1 Task 1.5 CORE: cross-process MAP_SHARED (smoke-shmshare)** — **M1 CLOSED** | PASS — memfd is now a real frame-backed anonymous object (kernel/Shm.h), not a /tmp file. New: SYS_memfd_create→Syscalls::memfdCreate; Fd.shm plumbing (fork/dup share via refcount, close frees frames at last ref); read/write/lseek/ftruncate/fstat route to the Shm (read/write bounce through a kernel-heap buffer across the kernel↔user CR3 gap); mmap(MAP_SHARED) maps the Shm's frames via arch::mmuMapUserSharedAt tagged **PTE_SHARED** (bit 10). Teardown-safe + fork-correct: freeUserWindow SKIPS freeing PTE_SHARED leaves and copyUserWindowFrom ALIASES them (frames owned by the Shm, freed only at last fd close) — no double-free, and inherited MAP_SHARED stays shared. Reuses the fb-window VA allocator (no new memory-map window, no Process .bss growth). Tests: test64 912 cases (test_shm.cpp 10 cases + test_addressspace64 PTE_SHARED skip/alias cases); smoke-shmshare (shmdualtest, -smp 4) PASS. No-regression: smoke-memfd/mprotect/eventfd/procself + smoke-x86_64 all PASS. Bug found+fixed mid-task: shmFrameAlloc's zeroing (and read/write frame access) ran under the caller CR3 → #PF (cr2 in the user window); frames are identity-mapped only under the kernel directory, so zero under kernel CR3 + bounce read/write. Limitation (documented): a mapping does not pin the Shm — keep the memfd fd open while mapped (as Chromium does). |

